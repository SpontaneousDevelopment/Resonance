name: ingest
description: Licence-checked ingestion of reference audio.
publish_to: none

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  http: ^1.2.0
  path: ^1.9.0
  crypto: ^3.0.0

dev_dependencies:
  test: ^1.31.1
