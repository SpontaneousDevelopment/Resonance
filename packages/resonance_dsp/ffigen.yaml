# Run with `dart run ffigen --config ffigen.yaml`.
name: ResonanceDspBindings
description: |
  Bindings for `src/resonance_dsp.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/resonance_dsp_bindings_generated.dart'
headers:
  entry-points:
    - 'src/resonance_dsp.h'
  include-directives:
    - 'src/resonance_dsp.h'
comments:
  style: any
  length: full
